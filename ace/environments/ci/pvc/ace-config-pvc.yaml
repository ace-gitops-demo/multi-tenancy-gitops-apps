apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ace-config-pvc
spec:
  storageClassName: ibmc-vpc-block-10iops-tier
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Block